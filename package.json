{
  "name": "sling-url-parser",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "mocha",
    "build": "babel src --out-dir lib --presets=@babel/env && uglifyjs --compress --mangle -o dist/SlingUrl.js -- lib/SlingUrl.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "mocha": "^6.0.2"
  },
  "dependencies": {
    "uglify-js": "^3.5.3"
  }
}
